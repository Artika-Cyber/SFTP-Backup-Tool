version: '3'
services:
  sbato:
    container_name: sbato
    image: artika-cyber/sbato:latest
    volumes:
      - ./config.yml:/app/config.yml
      - /home/alexis/Documents/HomeInfra/ssh:/root/.ssh
      - ./backups:/backups
    command: backup